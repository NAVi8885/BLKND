<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BLKND - Fashion Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Jost', sans-serif; margin: 0; padding: 0; }
        .hero-section {
            background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('https://images.unsplash.com/photo-1558769132-cb1aea1f1f41?w=1200');
            background-size: cover;
            background-position: center;
            height: 600px;
            display: flex;
            align-items: center;
            color: #fff;
        }
        .hero-content h1 { font-size: 56px; font-weight: 700; margin-bottom: 20px; }
        .category-card { position: relative; overflow: hidden; height: 300px; border-radius: 8px; margin-bottom: 30px; }
        .category-card img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.3s; }
        .category-card:hover img { transform: scale(1.1); }
        .category-overlay { position: absolute; bottom: 20px; left: 20px; color: #fff; }
        .product-card { border: none; transition: transform 0.3s; margin-bottom: 30px; }
        .product-card:hover { transform: translateY(-5px); }
        .product-card img { height: 350px; object-fit: cover; }
        .section-title { font-size: 36px; font-weight: 700; margin-bottom: 50px; text-align: center; }
        html { margin: 0; padding: 0; }

    .top-nav {
        position: absolute; /* Floats on top */
        top: 0;
        left: 0;
        width: 100%;
        z-index: 1031; /* Higher than navbar */
        background-color: transparent !important; /* Removes dark background */
        border-bottom: 1px solid rgba(255,255,255,0.1); /* Optional: subtle subtle divider */
    }

    /* 2. Make the Main Navbar Transparent & Float */
    .navbar {
        position: absolute; /* Floats on top */
        top: 40px; /* Pushes it down below the top-nav text */
        left: 0;
        width: 100%;
        z-index: 1030;
        background: transparent !important; /* Removes white background */
        box-shadow: none !important; /* Removes shadow */
    }

    /* 3. Ensure Text is White */
    .nav-link, .navbar-brand, .top-nav a, .top-nav span {
        color: #fff !important;
        text-shadow: 0 2px 4px rgba(0,0,0,0.5); /* Makes text readable on bright images */
    }

    /* 4. Full Screen Hero Settings */
    .full-screen-hero {
        position: relative;
        width: 100%;
        /* Use min-height to prevent content cutting off on small/landscape screens */
        min-height: 100vh; 
        overflow: hidden;
    }

    .carousel-item {
        /* Ensure the item itself takes up the full height */
        height: 100vh;
        min-height: 600px; /* Fallback for very short screens */
    }

    .banner-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
    }

    .hero-overlay {
        position: absolute;
        inset: 0;
        background: rgba(0,0,0,0.4); /* Slightly darker for better text readability */
        z-index: 1;
    }
    
    .hero-content {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        text-align: center;
        color: white;
        z-index: 2;
        width: 90%; /* Prevents text from hitting the screen edges */
        max-width: 900px; /* Keeps text constrained on large screens */
    }

    /* DEFAULT (Desktop) Typography */
    .hero-title {
        font-size: 4rem; /* Equivalent to display-3 but easier to control */
        font-weight: 700;
        margin-bottom: 1rem;
        text-transform: uppercase;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
    }

    .hero-subtitle {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
    }

    /* RESPONSIVE: Tablet & Mobile Tweaks */
    @media (max-width: 992px) {
        .hero-title {
            font-size: 3rem;
        }
    }

    @media (max-width: 768px) {
        .carousel-item {
            min-height: 500px; /* Allow shorter height on mobile if needed */
        }
        
        .hero-title {
            font-size: 2.2rem; /* Much smaller for mobile to prevent overflow */
            line-height: 1.2;
        }

        .hero-subtitle {
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
        }

        .btn-lg {
            padding: 10px 25px;
            font-size: 1rem;
        }
    }
</style>
    </style>
</head>
<body>
    <div class="top-nav text-white" style="background-color: #0e0a0ab4;  padding: 10px 0; font-size: 14px;">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-facebook"></i></a>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-instagram"></i></a>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-twitter"></i></a>
                </div>
                <div><span>Free shipping on orders over $50</span></div>
                <% if(!user){ %>
                    <div>
                        <a href="/login" style="color: #fff; text-decoration: none; margin: 0 15px;">Login</a>
                        <a href="/signup" style="color: #fff; text-decoration: none; margin: 0 15px;">Register</a>
                    </div>
                <% } else {%>
                    <div>
                        <p style="margin-bottom: 0rem;"><%= user.name %></p>
                    </div>
                    <% } %>
            </div>
        </div>
    </div>
    
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/index" style="font-size: 28px; font-weight: 700;">BLKND</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link active" href="/index">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/shop">Shop</a></li>
                    <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
                </ul>
                <div class="nav-icons d-flex align-items-center">
                    <a href="#" data-bs-toggle="modal" data-bs-target="#searchModal" style="color: #fff; font-size: 18px; margin-left: 20px;"><i class="fas fa-search"></i></a>
                    <a href="/profile" style="color: #fff; font-size: 18px; margin-left: 20px;"><i class="fas fa-user"></i></a>
                    <a href="/wishlist" style="color: #fff; font-size: 18px; margin-left: 20px;"><i class="fas fa-heart"></i></a>
                    <a href="/cart" style="color: #fff; font-size: 18px; margin-left: 20px; position: relative;">
                        <i class="fas fa-shopping-cart"></i>
                        <span style="position: absolute; top: -8px; right: -8px; background: #212529; color: #fff; border-radius: 50%; padding: 2px 6px; font-size: 10px;">0</span>
                    </a>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="modal fade" id="searchModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-body">
                    <button type="button" class="btn-close float-end" data-bs-dismiss="modal"></button>
                    <div class="mt-4">
                        <input type="search" class="form-control form-control-lg" placeholder="Search products...">
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="heroCarousel" class="carousel slide full-screen-hero" data-bs-ride="carousel">
        <div class="carousel-inner">
            <% if(banners && banners.length > 0) { %>
                <% banners.forEach((banner, index) => { %>
                    <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                        <img src="/uploads/banners/<%= banner.image %>" class="banner-img" alt="<%= banner.title %>">

                        <div class="hero-overlay"></div>

                        <div class="hero-content">
                            <h1 class="display-3 fw-bold text-uppercase"><%= banner.title %></h1>
                            <% if(banner.subtitle) { %>
                                <p class="fs-4 mb-4"><%= banner.subtitle %></p>
                            <% } %>
                            <% if(banner.link) { %>
                                <a href="<%= banner.link %>" class="btn btn-light btn-lg px-5 rounded-0">SHOP NOW</a>
                            <% } %>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>

        <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
        </button>
    </div>

    <section class="py-5 mt-5">
        <div class="container">
            <h2 class="section-title">Shop by Category</h2>
            <div class="row">
                <div class="col-md-4">
                    <a href="/shop" class="text-decoration-none">
                        <div class="category-card">
                            <img src="https://images.unsplash.com/photo-1490481651871-ab68de25d43d?w=500" alt="Women">
                            <div class="category-overlay"><h3>Women</h3></div>
                        </div>
                    </a>
                </div>
                <div class="col-md-4">
                    <a href="/shop" class="text-decoration-none">
                        <div class="category-card">
                            <img src="https://images.unsplash.com/photo-1516826957135-700dedea698c?w=500" alt="Men">
                            <div class="category-overlay"><h3>Men</h3></div>
                        </div>
                    </a>
                </div>
                <div class="col-md-4">
                    <a href="/shop" class="text-decoration-none">
                        <div class="category-card">
                            <img src="https://images.unsplash.com/photo-1503342217505-b0a15ec3261c?w=500" alt="Accessories">
                            <div class="category-overlay"><h3>Accessories</h3></div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </section>

    <section class="py-5 bg-light">
        <div class="container">
            <h2 class="section-title">Top Sellers</h2>

            <div class="row">
                <% if(products && products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <div class="col-md-3">
                            <div class="card product-card">
                                <a href="/product/<%= product._id %>" class="text-decoration-none text-dark">
                                    <img src="<%= product.image[0] %>" 
                                         class="card-img-top" 
                                         alt="<%= product.name %>">

                                    <div class="card-body text-center">
                                        <h5 class="card-title"><%= product.name %></h5>

                                        <p class="text-muted">₹<%= product.price %></p>

                                        <a href="/product/<%= product._id %>" class="btn btn-outline-dark btn-sm">View Details</a>
                                    </div>
                                </a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="col-12 text-center">
                        <p>No products available at the moment.</p>
                    </div>
                <% } %>
            </div>

            <div class="text-center mt-4">
                <a href="/shop" class="btn btn-dark btn-lg">View All Products</a>
            </div>
        </div>
    </section>  

    <section class="py-5 my-5 bg-dark text-white">
        <div class="container text-center">
            <h2 class="mb-3">Special Offer!</h2>
            <p class="lead">Get 20% off on your first order</p>
            <a href="/shop" class="btn btn-light btn-lg mt-3">Shop Now</a>
        </div>
    </section>

    <footer style="background-color: #212529; color: #fff; padding: 60px 0 20px;">
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h5 style="font-weight: 600; margin-bottom: 20px;">BLKND</h5>
                    <p style="color: #adb5bd;">Your premier destination for fashion and style.</p>
                    <div class="mt-3">
                        <a href="#" style="color: #adb5bd; text-decoration: none; margin-right: 15px;"><i class="fab fa-facebook fa-lg"></i></a>
                        <a href="#" style="color: #adb5bd; text-decoration: none; margin-right: 15px;"><i class="fab fa-instagram fa-lg"></i></a>
                        <a href="#" style="color: #adb5bd; text-decoration: none; margin-right: 15px;"><i class="fab fa-twitter fa-lg"></i></a>
                    </div>
                </div>
                <div class="col-md-3">
                    <h5 style="font-weight: 600; margin-bottom: 20px;">Quick Links</h5>
                    <ul style="list-style: none; padding: 0;">
                        <li style="margin-bottom: 10px;"><a href="/about" style="color: #adb5bd; text-decoration: none;">About Us</a></li>
                        <li style="margin-bottom: 10px;"><a href="/shop" style="color: #adb5bd; text-decoration: none;">Shop</a></li>
                        <li style="margin-bottom: 10px;"><a href="/contact" style="color: #adb5bd; text-decoration: none;">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5 style="font-weight: 600; margin-bottom: 20px;">Customer Service</h5>
                    <ul style="list-style: none; padding: 0;">
                        <li style="margin-bottom: 10px;"><a href="#" style="color: #adb5bd; text-decoration: none;">Track Order</a></li>
                        <li style="margin-bottom: 10px;"><a href="/profile" style="color: #adb5bd; text-decoration: none;">My Account</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5 style="font-weight: 600; margin-bottom: 20px;">Newsletter</h5>
                    <form>
                        <div class="input-group">
                            <input type="email" class="form-control" placeholder="Your email">
                            <button class="btn btn-light" type="submit">Subscribe</button>
                        </div>
                    </form>
                </div>
            </div>
            <hr style="border-color: rgba(255,255,255,0.1); margin: 30px 0;">
            <div class="row">
                <div class="col-md-12 text-center">
                    <p style="color: #adb5bd;">&copy; 2025 BLKND. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
