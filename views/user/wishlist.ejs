<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wishlist - BLKND</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Jost', sans-serif; }
        .page-header { background-color: #f8f9fa; padding: 80px 0 40px; margin-bottom: 60px; }
        .wishlist-item { border-bottom: 1px solid #dee2e6; padding: 20px 0; transition: background-color 0.2s; }
        .wishlist-item:hover { background-color: #fafafa; }
        .wishlist-item img { width: 120px; height: 120px; object-fit: cover; border-radius: 8px; }
        .stock-badge { font-size: 0.85rem; font-weight: 500; }
        .text-success { color: #198754 !important; }
        .text-danger { color: #dc3545 !important; }
    </style>
</head>
<body>
    <div class="top-nav text-white" style="background-color: #212529; padding: 10px 0; font-size: 14px;">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-facebook"></i></a>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-instagram"></i></a>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-twitter"></i></a>
                </div>
                <div class="d-none d-md-block"><span>Free shipping on orders over ₹2000</span></div>
                <div>
                    <% if (user) { %>
                        <a href="/profile" style="color: #fff; text-decoration: none; margin: 0 15px;">My Account</a>
                        <a href="/logout" style="color: #fff; text-decoration: none; margin: 0 15px;">Logout</a>
                    <% } else { %>
                        <a href="/login" style="color: #fff; text-decoration: none; margin: 0 15px;">Login</a>
                        <a href="/signup" style="color: #fff; text-decoration: none; margin: 0 15px;">Register</a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top" style="padding: 20px 0; box-shadow: 0 2px 5px rgba(0,0,0,0.1);">
        <div class="container">
            <a class="navbar-brand" href="/" style="font-size: 28px; font-weight: 700;">BLKND</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="/index">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/shop">Shop</a></li>
                    <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
                </ul>
                <div class="nav-icons d-flex align-items-center">
                    <a href="/shop" style="color: #212529; font-size: 18px; margin-left: 20px;"><i class="fas fa-search"></i></a>
                    <a href="/profile" style="color: #212529; font-size: 18px; margin-left: 20px;"><i class="fas fa-user"></i></a>
                    <a href="/wishlist" style="color: #212529; font-size: 18px; margin-left: 20px;"><i class="fas fa-heart"></i></a>
                    <a href="/cart" style="color: #212529; font-size: 18px; margin-left: 20px; position: relative;">
                        <i class="fas fa-shopping-cart"></i>
                        </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="page-header">
        <div class="container">
            <h1 class="mb-2">Wishlist</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/index" class="text-decoration-none text-muted">Home</a></li>
                    <li class="breadcrumb-item active">Wishlist</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="container mb-5">
        
        <% if (wishlist && wishlist.products.length > 0) { %>
            
            <% wishlist.products.forEach(item => { %>
                <% if(item.productId) { %>
                <div class="wishlist-item">
                    <div class="row align-items-center">
                        <div class="col-md-2 text-center text-md-start mb-3 mb-md-0">
                            <a href="/product/<%= item.productId._id %>">
                                <img src="<%= item.productId.image && item.productId.image[0] ? item.productId.image[0] : '/images/placeholder.png' %>" alt="<%= item.productId.name %>">
                            </a>
                        </div>
                        
                        <div class="col-md-5 mb-3 mb-md-0">
                            <h5 class="mb-1">
                                <a href="/product/<%= item.productId._id %>" class="text-dark text-decoration-none">
                                    <%= item.productId.name %>
                                </a>
                            </h5>
                            <div class="text-muted small mb-2">
                                <span class="me-3">Size: <strong><%= item.size %></strong></span>
                                <span>Color: <strong><%= item.color %></strong></span>
                            </div>
                            
                            <div class="mb-2">
                                <i class="fas fa-star text-warning small"></i>
                                <i class="fas fa-star text-warning small"></i>
                                <i class="fas fa-star text-warning small"></i>
                                <i class="fas fa-star text-warning small"></i>
                                <i class="far fa-star text-warning small"></i>
                            </div>
                        </div>

                        <div class="col-md-2 mb-3 mb-md-0">
                            <p class="fw-bold mb-0">₹<%= item.productId.price %></p>
                            <% if (item.productId.stock > 0) { %>
                                <p class="stock-badge text-success mb-0">In Stock</p>
                            <% } else { %>
                                <p class="stock-badge text-danger mb-0">Out of Stock</p>
                            <% } %>
                        </div>

                        <div class="col-md-3 text-md-end d-flex d-md-block align-items-center justify-content-between">
                            <% if (item.productId.stock > 0) { %>
                                <a href="/product/<%= item.productId._id %>" class="btn btn-dark btn-sm">View Product</a>
                            <% } else { %>
                                <button class="btn btn-secondary btn-sm" disabled>Unavailable</button>
                            <% } %>

                            <form action="/remove-from-wishlist" method="POST" class="d-inline-block ms-2">
                                <input type="hidden" name="productId" value="<%= item.productId._id %>">
                                <button type="submit" class="btn btn-link text-danger btn-sm p-0" title="Remove">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
                <% } %>
            <% }) %>

            <div class="mt-5">
                <a href="/shop" class="btn btn-outline-dark px-4 py-2">Continue Shopping</a>
            </div>

        <% } else { %>
            
            <div class="text-center py-5">
                <div class="mb-4">
                    <i class="far fa-heart" style="font-size: 60px; color: #dee2e6;"></i>
                </div>
                <h3>Your wishlist is empty</h3>
                <p class="text-muted mb-4">Seems like you haven't found anything you like yet.</p>
                <a href="/shop" class="btn btn-dark btn-lg px-5">Start Shopping</a>
            </div>

        <% } %>
    </div>

    <footer style="background-color: #212529; color: #fff; padding: 60px 0 20px;">
        <div class="container">
            <div class="row g-4">
                <div class="col-md-3">
                    <h5 class="fw-bold mb-3">BLKND</h5>
                    <p style="color: #adb5bd; font-size: 0.9rem;">Your premier destination for fashion and style. Redefining your wardrobe with quality and elegance.</p>
                </div>
                <div class="col-md-3">
                    <h5 class="fw-bold mb-3">Quick Links</h5>
                    <ul style="list-style: none; padding: 0;">
                        <li class="mb-2"><a href="/about" style="color: #adb5bd; text-decoration: none;">About Us</a></li>
                        <li class="mb-2"><a href="/shop" style="color: #adb5bd; text-decoration: none;">Shop</a></li>
                        <li class="mb-2"><a href="/contact" style="color: #adb5bd; text-decoration: none;">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5 class="fw-bold mb-3">Customer Service</h5>
                    <ul style="list-style: none; padding: 0;">
                        <li class="mb-2"><a href="/profile" style="color: #adb5bd; text-decoration: none;">My Account</a></li>
                        <li class="mb-2"><a href="/cart" style="color: #adb5bd; text-decoration: none;">Shopping Cart</a></li>
                        <li class="mb-2"><a href="#" style="color: #adb5bd; text-decoration: none;">Terms & Conditions</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5 class="fw-bold mb-3">Newsletter</h5>
                    <form>
                        <div class="input-group">
                            <input type="email" class="form-control" placeholder="Your email">
                            <button class="btn btn-light" type="button">Subscribe</button>
                        </div>
                    </form>
                </div>
            </div>
            <hr style="border-color: rgba(255,255,255,0.1); margin: 30px 0;">
            <div class="text-center">
                <p style="color: #adb5bd; font-size: 0.85rem;">&copy; 2025 BLKND. All rights reserved.</p>
            </div>
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>