<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>BLKND • Reset Password</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body { min-height:100vh; background:#f8f9fa; font-family:system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;
      display:flex; align-items:center; justify-content:center; }
    .card { box-shadow: 0 6px 32px rgba(33,37,41,.08); }
    .brand { font-weight:700; font-size:2rem; color:#212529; text-decoration:none; }
    .reset-box { max-width:380px; width:100%; }
    .form-floating label { font-size:.97rem; }
    .show-pass { cursor:pointer; position:absolute; right:0; top:0; height:100%; display:flex; align-items:center; padding: 0 1rem; }
  </style>
</head>
<body>
  <div class="container d-flex align-items-center justify-content-center" style="min-height:100vh;">
    <div class="reset-box">
      <div class="text-center mb-3">
        <a href="/index" class="brand">BLKND</a>
        <div class="fw-semibold mb-2 text-muted">Reset your password</div>
      </div>
      <div class="card">
        <div class="card-body p-4">
          <form action="/resetpassword" method="POST" autocomplete="off">
            <div class="mb-4 text-center">
              <i class="fa-solid fa-key fa-2x mb-2 text-muted"></i>
              <div class="fw-semibold mb-2">Enter your new password below</div>
            </div>
            <div class="form-floating mb-3 position-relative">
              <input type="password" id="newPass" name="new_password" class="form-control" placeholder="New password" required minlength="5">
              <label for="newPass"><i class="fa-solid fa-lock me-1"></i> New Password</label>

              <input type="hidden" name="email" value="<%= email %>" /> 

              <span class="show-pass" onclick="togglePass('newPass','npIcon')" tabindex="0">
                <i class="fa-regular fa-eye" id="npIcon"></i>
              </span>
            </div>
            <div class="form-floating mb-3 position-relative">
              <input type="password" id="confirmPass" name="confirm_password" class="form-control" placeholder="Confirm password" required minlength="5">
              <label for="confirmPass"><i class="fa-solid fa-lock me-1"></i> Confirm Password</label>
              <span class="show-pass" onclick="togglePass('confirmPass','cpIcon')" tabindex="0">
                <i class="fa-regular fa-eye" id="cpIcon"></i>
              </span>
            </div>
            <button type="submit" class="btn btn-dark w-100 fw-bold py-2 mb-2">Reset Password</button>
          </form>
          <div id="msg" class="text-center text-danger small" style="display:none;"></div>
          <div class="text-center mt-3">
            <a href="/login" class="text-decoration-none text-muted"><i class="fa-solid fa-arrow-left me-1"></i> Back to Login</a>
          </div>
        </div>
      </div>
      <div class="text-center text-muted mt-3" style="font-size:13px;">© 2025 BLKND</div>
    </div>
  </div>
  <script>
    function togglePass(inputId, iconId) {
      const input = document.getElementById(inputId);
      const icon = document.getElementById(iconId);
      if(input.type === "password") { input.type = "text"; icon.classList.replace('fa-eye', 'fa-eye-slash'); }
      else { input.type = "password"; icon.classList.replace('fa-eye-slash', 'fa-eye'); }
    }
    // (Optional basic client validation for matching passwords)
    document.querySelector('form').onsubmit = function(e){
      var np = document.getElementById('newPass').value;
      var cp = document.getElementById('confirmPass').value;
      var msg = document.getElementById('msg');
      if(np !== cp){
        msg.textContent = "Passwords do not match."; msg.style.display = "block";
        e.preventDefault(); return false;
      }
      if(np.length < 6){
        msg.textContent = "Password must be at least 6 characters."; msg.style.display = "block";
        e.preventDefault(); return false;
      }
    }
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
