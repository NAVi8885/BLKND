<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop - BLKND</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        body { font-family: 'Jost', sans-serif; }
        .page-header { background-color: #f8f9fa; padding: 80px 0 40px; margin-bottom: 60px; }
        .product-card { border: none; transition: transform 0.3s; margin-bottom: 30px; }
        .product-card:hover { transform: translateY(-5px); }
        .product-card img { height: 350px; object-fit: cover; width: 100%; }
        .filter-sidebar { position: sticky; top: 100px; }

        /* RESPONSIVE FIXES */
        @media (max-width: 991.98px) {
            .product-card img { height: 280px; }
            .page-header { padding: 60px 0 30px; }
        }

        @media (max-width: 575.98px) {
            .top-nav .d-flex { flex-direction: column; align-items: flex-start; gap: 8px; }
            .filter-sidebar { position: static; margin-bottom: 20px; }
            .product-card img { height: 220px; }
        }
        .filter-sidebar {
  font-family: system-ui, -apple-system, BlinkMacSystemFont;
}

.zara-category-link {
  text-decoration: none;
  color: #000;
  font-size: 14px;
  letter-spacing: 0.3px;
}

.zara-category-link:hover {
  text-decoration: underline;
}

    </style>
</head>

<body>

    <!-- TOP BAR -->
    <div class="top-nav text-white" style="background-color: #212529; padding: 10px 0; font-size: 14px;">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-facebook"></i></a>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-instagram"></i></a>
                    <a href="#" style="color: #fff; text-decoration: none; margin: 0 15px;"><i class="fab fa-twitter"></i></a>
                </div>
                <div><span>Free shipping on orders over $50</span></div>
                <% if(!user){ %>
                    <div>
                        <a href="/login" style="color: #fff; text-decoration: none; margin: 0 15px;">Login</a>
                        <a href="/signup" style="color: #fff; text-decoration: none; margin: 0 15px;">Register</a>
                    </div>
                <% } else {%>
                    <div>
                        <p style="margin-bottom: 0rem;"><%= user.name %></p>
                    </div>
                    <% } %>
            </div>
        </div>
    </div>

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top" style="padding: 20px 0; box-shadow: 0 2px 5px rgba(0,0,0,0.1);">
        <div class="container">
            <a class="navbar-brand" href="/index" style="font-size: 28px; font-weight: 700;">BLKND</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">

                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="/index">Home</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/shop">Shop</a></li>
                    <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
                </ul>

                <div class="nav-icons d-flex align-items-center">
                    <a href="#" data-bs-toggle="modal" data-bs-target="#searchModal" style="color: #212529; font-size: 18px; margin-left: 20px;"><i class="fas fa-search"></i></a>
                    <a href="/profile" style="color: #212529; font-size: 18px; margin-left: 20px;"><i class="fas fa-user"></i></a>
                    <a href="/wishlist" style="color: #212529; font-size: 18px; margin-left: 20px;"><i class="fas fa-heart"></i></a>
                    <a href="/cart" style="color: #212529; font-size: 18px; margin-left: 20px; position: relative;">
                        <i class="fas fa-shopping-cart"></i>
                        <span style="position: absolute; top: -8px; right: -8px; background: #212529; color: #fff; border-radius: 50%; padding: 2px 6px; font-size: 10px;">0</span>
                    </a>
                </div>

            </div>
        </div>
    </nav>

    <!-- SEARCH MODAL -->
    <div class="modal fade" id="searchModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-body">
                    <button type="button" class="btn-close float-end" data-bs-dismiss="modal"></button>
                    <div class="mt-4">
                        <input type="search" class="form-control form-control-lg" placeholder="Search products...">
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- PAGE HEADER -->
    <div class="page-header">
        <div class="container">
            <h1 class="mb-2">Shop</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/index">Home</a></li>
                    <li class="breadcrumb-item active">Shop</li>
                </ol>
            </nav>
        </div>
    </div>

    <!-- MAIN CONTENT -->
    <div class="container mb-5">
        <div class="row">

            <!-- SIDEBAR -->
<div class="col-lg-3">
  <aside class="filter-sidebar">

    <!-- CATEGORIES (ZARA STYLE) -->
    <div class="mb-5">
      
      <div class="mb-4">
        <a href="/shop" 
           class="text-uppercase fw-semibold mb-2 d-block text-decoration-none <%= !selectedCategory ? 'text-primary' : 'text-dark' %>"
           style="font-size:13px; letter-spacing:1px;">
           All Products
        </a>
      </div>

      <% categories.forEach(cat => { %>
        <div class="mb-4">

          <a href="/shop?category=<%= encodeURIComponent(cat.main) %>"
             class="text-uppercase fw-semibold mb-2 d-block text-decoration-none <%= selectedCategory === cat.main && !selectedSubcategory ? 'text-primary' : 'text-dark' %>"
             style="font-size:13px; letter-spacing:1px;"
          >
            <%= cat.main %>
          </a>

          <ul class="list-unstyled m-0 p-0 ps-3">
            <% cat.sub.forEach(sub => { %>
              <li class="mb-1">
                <a
                  href="/shop?category=<%= encodeURIComponent(cat.main) %>&subcategory=<%= encodeURIComponent(sub) %>"
                  class="zara-category-link <%= selectedSubcategory === sub ? 'fw-bold text-dark' : 'text-muted' %>"
                >
                  <%= sub %>
                </a>
              </li>
            <% }) %>
          </ul>

        </div>
      <% }) %>

    </div>

    <!-- PRICE RANGE -->
    <div class="mb-5">
                        <div class="fw-semibold mb-3">Price Range</div>
                        
                        <div class="price-values">
                            <span id="minPriceDisplay">₹<%= selectedMinPrice %></span>
                            <span id="maxPriceDisplay">₹<%= selectedMaxPrice %></span>
                        </div>

                        <div class="price-slider-wrapper">
                            <div class="slider-track"></div>
                            <input type="range" class="range-input" id="minRange" min="0" max="10000" step="100" value="<%= selectedMinPrice %>">
                            <input type="range" class="range-input" id="maxRange" min="0" max="10000" step="100" value="<%= selectedMaxPrice %>">
                        </div>
                        
                        <button class="btn btn-dark btn-sm w-100 mt-3" onclick="applyFilters()">Filter</button>
                    </div>

    <!-- SIZE -->
    <div>
      <div class="fw-semibold mb-3">Size</div>

      <div class="form-check mb-1">
        <input class="form-check-input" type="checkbox" id="sizeXS">
        <label class="form-check-label" for="sizeXS">XS</label>
      </div>

      <div class="form-check mb-1">
        <input class="form-check-input" type="checkbox" id="sizeS">
        <label class="form-check-label" for="sizeS">S</label>
      </div>

      <div class="form-check mb-1">
        <input class="form-check-input" type="checkbox" id="sizeM">
        <label class="form-check-label" for="sizeM">M</label>
      </div>

      <div class="form-check mb-1">
        <input class="form-check-input" type="checkbox" id="sizeL">
        <label class="form-check-label" for="sizeL">L</label>
      </div>

      <div class="form-check">
        <input class="form-check-input" type="checkbox" id="sizeXL">
        <label class="form-check-label" for="sizeXL">XL</label>
      </div>

    </div>

  </aside>
</div>


            <!-- PRODUCT LIST -->
            <div class="col-lg-9">

                <div class="d-flex justify-content-between align-items-center mb-4">
                    <p class="mb-0">Products</p>
                    <select class="form-select w-auto">
                        <option>Sort by latest</option>
                        <option>Price: Low to High</option>
                        <option>Price: High to Low</option>
                        <option>Name: A to Z</option>
                    </select>
                </div>

                <!-- DYNAMIC PRODUCT GRID -->
                <p class="mb-0 text-muted"><span id="productCount"><%= products.length %></span> Products found</p>
                <div class="row" id="productGrid">

                    <% if (products && products.length > 0) { %>

                        <% products.forEach((products, index) =>{ %>
                            <div class="col-6 col-md-4">
                                <div class="card product-card">

                                    <img
                                        src="<%= products.image && products.image.length ? products.image[0] : '/images/placeholder.png' %>"
                                        alt="<%= products.name %>"
                                        >

                                    <div class="card-body text-center">
                                        <h5 class="card-title"><%= products.name %></h5>
                                        <p class="text-muted">₹<%= products.price %></p>

                                        <a href="/product/<%= products._id %>" 
                                           class="btn btn-outline-dark btn-sm">
                                           View Details
                                        </a>
                                    </div>

                                </div>
                            </div>
                        <% }) %>

                    <% } else { %>

                        <div class="col-12 text-center">
                            <p class="text-muted">No products available.</p>
                        </div>

                    <% } %>

                </div>

            </div>
        </div>
    </div>

    <!-- FOOTER -->
    <footer style="background-color: #212529; color: #fff; padding: 60px 0 20px;">
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h5>BLKND</h5>
                    <p style="color: #adb5bd;">Your premier destination for fashion and style.</p>
                </div>
                <div class="col-md-3">
                    <h5>Quick Links</h5>
                    <ul style="list-style: none; padding: 0;">
                        <li><a href="/about" style="color: #adb5bd;">About Us</a></li>
                        <li><a href="/shop" style="color: #adb5bd;">Shop</a></li>
                        <li><a href="/contact" style="color: #adb5bd;">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Customer Service</h5>
                    <ul style="list-style: none; padding: 0;">
                        <li><a href="/profile" style="color: #adb5bd;">My Account</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Newsletter</h5>
                    <form>
                        <div class="input-group">
                            <input type="email" class="form-control" placeholder="Your email">
                            <button class="btn btn-light" type="submit">Subscribe</button>
                        </div>
                    </form>
                </div>
            </div>

            <hr style="border-color: rgba(255,255,255,0.1); margin: 30px 0;">
            <div class="text-center">
                <p style="color: #adb5bd;">&copy; 2025 BLKND. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
    // 1. Slider Variables
    const minRange = document.getElementById("minRange");
    const maxRange = document.getElementById("maxRange");
    const minDisplay = document.getElementById("minPriceDisplay");
    const maxDisplay = document.getElementById("maxPriceDisplay");

    // 2. Slider UI Logic (Visual Only)
    function updateSliderUI(e) {
        let minVal = parseInt(minRange.value);
        let maxVal = parseInt(maxRange.value);

        if (maxVal - minVal < 500) {
            if (event.target === minRange) {
                minRange.value = maxVal - 500;
            } else {
                maxRange.value = minVal + 500;
            }
        }
        minDisplay.innerText = "₹" + minRange.value;
        maxDisplay.innerText = "₹" + maxRange.value;
    }

    // Attach Input Listeners for visual updates
    minRange.addEventListener("input", updateSliderUI);
    maxRange.addEventListener("input", updateSliderUI);

    // 3. MAIN AJAX FUNCTION
    async function applyFilters() {
        // A. Gather all current values
        const params = new URLSearchParams(window.location.search);
        
        // Update params with current inputs
        params.set('minPrice', minRange.value);
        params.set('maxPrice', maxRange.value);
        params.set('sort', document.getElementById('sortSelect').value);
        
        // Ensure we keep category/search if they exist in URL
        // (params object already grabbed them from window.location.search)

        // B. Update the Browser URL (without reloading)
        const newRelativePathQuery = window.location.pathname + '?' + params.toString();
        window.history.pushState(null, '', newRelativePathQuery);

        // C. Fetch Data
        try {
            // Add 'ajax=true' for the backend
            params.set('ajax', 'true');
            
            const response = await fetch(`/shop?${params.toString()}`, {
                method: 'GET',
                headers: { 'X-Requested-With': 'XMLHttpRequest' }
            });

            if (!response.ok) throw new Error('Network response was not ok');

            const data = await response.json();

            if (data.success) {
                renderProducts(data.products);
            }

        } catch (error) {
            console.error('Filter Error:', error);
        }
    }

    // 4. Render HTML dynamically
    function renderProducts(product) {
        const grid = document.getElementById('productGrid');
        const countSpan = document.getElementById('productCount');
        
        // Update Count
        countSpan.innerText = product.length;

        // Clear Grid
        grid.innerHTML = '';

        if (product.length === 0) {
            grid.innerHTML = `
                <div class="col-12 text-center py-5">
                    <h4>No products found</h4>
                    <p class="text-muted">Try adjusting your filters.</p>
                </div>`;
            return;
        }

        // Build HTML for each product
        products.forEach(product => {
            // Handle image fallback safely
            const imageSrc = (product.image && product.image.length > 0) 
                ? product.image[0] 
                : '/images/placeholder.png';

            const html = `
                <div class="col-6 col-md-4">
                    <div class="card product-card">
                        <a href="/product/${product._id}">
                            <img src="${imageSrc}" alt="${product.name}" style="height: 350px; object-fit: cover; width: 100%;">
                        </a>
                        <div class="card-body text-center">
                            <h5 class="card-title" style="font-size: 16px;">${product.name}</h5>
                            <p class="text-muted mb-2">₹${product.price}</p>
                            <a href="/product/${product._id}" class="btn btn-outline-dark btn-sm">View Details</a>
                        </div>
                    </div>
                </div>
            `;
            grid.innerHTML += html;
        });
    }

    // 5. Intercept Category Links (Optional but Recommended)
    // This makes clicking sidebar categories ALSO use AJAX
    document.querySelectorAll('.filter-sidebar a').forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault(); // Stop full reload
            
            // Update URL to the link's href
            window.history.pushState(null, '', this.href);
            
            // Trigger the AJAX fetch using the new URL's params
            // We reset slider values to defaults if switching category, 
            // or we could read them from the DOM. Let's just re-run applyFilters logic
            // based on the new URL.
            
            // To be safe, we just manually call the fetch with the new URL params
            const url = new URL(this.href, window.location.origin);
            const params = new URLSearchParams(url.search);
            
            // Update UI inputs to match (reset sliders if needed, etc)
            // Ideally, you keep the sliders where they are or reset them.
            // For simplicity, let's just trigger the fetch:
            
            params.set('ajax', 'true');
            // Preserve current price slider values? 
            // Usually category change resets filters or keeps them. 
            // Let's stick to exactly what the link had.

            fetch(`/shop?${params.toString()}`)
                .then(res => res.json())
                .then(data => {
                    if (data.success) renderProducts(data.products);
                });
        });
    });
</script>
</body>
</html>
